import React from 'react';

import { connect } from 'react-redux';

import UserInfoSnippet from '../../components/UserInfo/UserInfoSnippet.jsx';

const mapStateToProps = state => {
    return {};
}

export default connect(mapStateToProps)(UserInfoSnippet);
