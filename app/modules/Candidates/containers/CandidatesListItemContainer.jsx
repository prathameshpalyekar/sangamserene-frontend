import { connect } from 'react-redux';

import CandidatesListItem from '../components/CandidatesListItem.jsx';

const mapStateToProps = (state, ownProps) => {
    return {
    };
};

const mapDispatchToProps = (dispatch, props) => {
    return {
    };
};

export default connect(mapStateToProps, mapDispatchToProps)(CandidatesListItem);
